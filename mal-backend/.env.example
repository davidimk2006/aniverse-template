# Example environment variables for MAL backend
MAL_CLIENT_ID=5fb5baaf6fe9088e1f261f796db6ffdc
MAL_CLIENT_SECRET=23148e56ceaf2896c099cb2e6896f3f4e06d5d1cfdfe4480b3d58aaaf3798c49
PORT=5000

# Admin token for upload endpoints (keep secret)
ADMIN_TOKEN=changeme

# Optional Supabase (recommended for Vercel deployments)
# Create a table named `videos` with columns matching the metadata below. Example SQL:
#
# CREATE TABLE public.videos (
#   id text PRIMARY KEY,
#   animeId text,
#   episode integer,
#   title text,
#   embed text,
#   download text,
#   quality text,
#   uploadedAt timestamptz
# );
#
SUPABASE_URL=https://awmczzjgnzdntlhobqkp.supabase.co
SUPABASE_KEY=sb_publishable_KPBJCBpYZFXAMYmCdp2-Rw_7Edn4k6P

# Do NOT commit real client secrets to public repos. Keep this file local and add .env to .gitignore.
